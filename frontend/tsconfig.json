{
  "compilerOptions": {
    "baseUrl": "./", // Setting the base URL
    "paths": {
      // Setting up the alias
      "@/*": ["src/*"]
    }
  },
  "files": [], // An empty "files" array, as specified
  "references": [
    // Project references for multiple tsconfigs
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ]
}
